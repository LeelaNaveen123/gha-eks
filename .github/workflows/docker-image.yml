name: Kubernetes CI
on:
  push:
    branches:
      - main

jobs:
  pyapp-ci:
    runs-on: ubuntu-latest
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4
      - name: Build docker image
        run: "docker build -t leelanaveen123/pyapp:$GITHUB_SHA ."
      
           
